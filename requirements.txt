# 换电脑重装时运行：python -m pip install -r requirements.txt
#
# 版本写死（==）而不是写 >=，是为了让"本地能跑"等于"云端能跑"：
# 云平台会按这个文件装依赖，如果写成 >=，它可能装到更新的版本而把页面搞崩。
pandas==3.0.5
requests==2.34.2
matplotlib==3.11.2
streamlit==1.63.0
